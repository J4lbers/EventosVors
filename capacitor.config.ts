import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vors.dev',
  appName: 'vorssorteos',
  webDir: 'dist/sorteos-vors/browser'
};

export default config;
